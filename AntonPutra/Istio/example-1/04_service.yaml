---
apiVersion: v1
kind: Service
metadata:
  name: first-app
  namespace: staging
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
  selector:
    app: first-app # it randomly route the traffic both version v1 and v2